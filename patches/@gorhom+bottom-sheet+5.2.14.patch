diff --git a/node_modules/@gorhom/bottom-sheet/lib/commonjs/components/bottomSheet/BottomSheet.js b/node_modules/@gorhom/bottom-sheet/lib/commonjs/components/bottomSheet/BottomSheet.js
index a7748c0..bad6c3c 100644
--- a/node_modules/@gorhom/bottom-sheet/lib/commonjs/components/bottomSheet/BottomSheet.js
+++ b/node_modules/@gorhom/bottom-sheet/lib/commonjs/components/bottomSheet/BottomSheet.js
@@ -764,6 +764,33 @@ const BottomSheetComponent = /*#__PURE__*/(0, _react.forwardRef)(function Bottom
      */
     animateToPosition(proposedPosition, source, undefined, animationConfigs);
   }, [getEvaluatedPosition, animateToPosition, setToPosition, reduceMotion, animateOnMount, animatedAnimationState, animatedContainerHeightDidChange, animatedCurrentIndex, animatedIndex, animatedPosition, animatedDetentsState, didAnimateOnMount, isInTemporaryPosition, isLayoutCalculated]);
+
+  /**
+   * Ensure the mount position evaluation runs even when the animated
+   * reaction that normally triggers it never fires.
+   */
+  (0, _react.useEffect)(() => {
+    let attempts = 0;
+    const enforceMountPosition = setInterval(() => {
+      if (didAnimateOnMount.value || attempts >= 20) {
+        clearInterval(enforceMountPosition);
+        return;
+      }
+      attempts += 1;
+      (0, _reactNativeReanimated.runOnUI)(() => {
+        'worklet';
+
+        if (didAnimateOnMount.value) {
+          return;
+        }
+        if (animatedAnimationState.get().status === _constants.ANIMATION_STATUS.RUNNING) {
+          return;
+        }
+        evaluatePosition(_constants.ANIMATION_SOURCE.MOUNT);
+      })();
+    }, 100);
+    return () => clearInterval(enforceMountPosition);
+  }, []);
   //#endregion
 
   //#region public methods
@@ -1268,6 +1295,7 @@ const BottomSheetComponent = /*#__PURE__*/(0, _react.forwardRef)(function Bottom
           containerLayoutState: containerLayoutState,
           topInset: topInset,
           bottomInset: bottomInset,
+          modal: $modal,
           detached: detached,
           style: _providedContainerStyle,
           children: /*#__PURE__*/(0, _jsxRuntime.jsxs)(_BottomSheetBody.BottomSheetBody, {
diff --git a/node_modules/@gorhom/bottom-sheet/lib/commonjs/components/bottomSheetHostingContainer/BottomSheetHostingContainer.js b/node_modules/@gorhom/bottom-sheet/lib/commonjs/components/bottomSheetHostingContainer/BottomSheetHostingContainer.js
index 350b8cc..be0254b 100644
--- a/node_modules/@gorhom/bottom-sheet/lib/commonjs/components/bottomSheetHostingContainer/BottomSheetHostingContainer.js
+++ b/node_modules/@gorhom/bottom-sheet/lib/commonjs/components/bottomSheetHostingContainer/BottomSheetHostingContainer.js
@@ -15,6 +15,7 @@ function BottomSheetHostingContainerComponent({
   layoutState,
   topInset = 0,
   bottomInset = 0,
+  modal,
   shouldCalculateHeight = true,
   detached,
   style,
@@ -53,6 +54,7 @@ function BottomSheetHostingContainerComponent({
         'worklet';
 
         state.rawContainerHeight = height;
+        state.containerHeight = modal ? height - topInset - bottomInset : height;
         return state;
       });
     }
@@ -91,7 +93,7 @@ function BottomSheetHostingContainerComponent({
         }
       });
     }
-  }, [layoutState, containerLayoutState]);
+  }, [layoutState, containerLayoutState, modal, topInset, bottomInset]);
   //#endregion
 
   //#region render
diff --git a/node_modules/@gorhom/bottom-sheet/lib/commonjs/components/bottomSheetModal/BottomSheetModal.js b/node_modules/@gorhom/bottom-sheet/lib/commonjs/components/bottomSheetModal/BottomSheetModal.js
index ee51e76..3b7f40f 100644
--- a/node_modules/@gorhom/bottom-sheet/lib/commonjs/components/bottomSheetModal/BottomSheetModal.js
+++ b/node_modules/@gorhom/bottom-sheet/lib/commonjs/components/bottomSheetModal/BottomSheetModal.js
@@ -191,6 +191,13 @@ function BottomSheetModalComponent(props, ref) {
       });
     }
 
+    /**
+     * Exit if the modal has never been presented.
+     */
+    if (statusRef.current === _constants.MODAL_STATUS.INITIAL) {
+      return;
+    }
+
     /**
      * if the modal position is already in a closed position,
      * then we unmount the node and early exit.
diff --git a/node_modules/@gorhom/bottom-sheet/lib/module/components/bottomSheet/BottomSheet.js b/node_modules/@gorhom/bottom-sheet/lib/module/components/bottomSheet/BottomSheet.js
index ca4bb3b..c652583 100644
--- a/node_modules/@gorhom/bottom-sheet/lib/module/components/bottomSheet/BottomSheet.js
+++ b/node_modules/@gorhom/bottom-sheet/lib/module/components/bottomSheet/BottomSheet.js
@@ -757,6 +757,33 @@ const BottomSheetComponent = /*#__PURE__*/forwardRef(function BottomSheet(props,
      */
     animateToPosition(proposedPosition, source, undefined, animationConfigs);
   }, [getEvaluatedPosition, animateToPosition, setToPosition, reduceMotion, animateOnMount, animatedAnimationState, animatedContainerHeightDidChange, animatedCurrentIndex, animatedIndex, animatedPosition, animatedDetentsState, didAnimateOnMount, isInTemporaryPosition, isLayoutCalculated]);
+
+  /**
+   * Ensure the mount position evaluation runs even when the animated
+   * reaction that normally triggers it never fires.
+   */
+  useEffect(() => {
+    let attempts = 0;
+    const enforceMountPosition = setInterval(() => {
+      if (didAnimateOnMount.value || attempts >= 20) {
+        clearInterval(enforceMountPosition);
+        return;
+      }
+      attempts += 1;
+      runOnUI(() => {
+        'worklet';
+
+        if (didAnimateOnMount.value) {
+          return;
+        }
+        if (animatedAnimationState.get().status === ANIMATION_STATUS.RUNNING) {
+          return;
+        }
+        evaluatePosition(ANIMATION_SOURCE.MOUNT);
+      })();
+    }, 100);
+    return () => clearInterval(enforceMountPosition);
+  }, []);
   //#endregion
 
   //#region public methods
@@ -1261,6 +1288,7 @@ const BottomSheetComponent = /*#__PURE__*/forwardRef(function BottomSheet(props,
           containerLayoutState: containerLayoutState,
           topInset: topInset,
           bottomInset: bottomInset,
+          modal: $modal,
           detached: detached,
           style: _providedContainerStyle,
           children: /*#__PURE__*/_jsxs(BottomSheetBody, {
diff --git a/node_modules/@gorhom/bottom-sheet/lib/module/components/bottomSheetHostingContainer/BottomSheetHostingContainer.js b/node_modules/@gorhom/bottom-sheet/lib/module/components/bottomSheetHostingContainer/BottomSheetHostingContainer.js
index a088ef2..38b52a4 100644
--- a/node_modules/@gorhom/bottom-sheet/lib/module/components/bottomSheetHostingContainer/BottomSheetHostingContainer.js
+++ b/node_modules/@gorhom/bottom-sheet/lib/module/components/bottomSheetHostingContainer/BottomSheetHostingContainer.js
@@ -10,6 +10,7 @@ function BottomSheetHostingContainerComponent({
   layoutState,
   topInset = 0,
   bottomInset = 0,
+  modal,
   shouldCalculateHeight = true,
   detached,
   style,
@@ -48,6 +49,7 @@ function BottomSheetHostingContainerComponent({
         'worklet';
 
         state.rawContainerHeight = height;
+        state.containerHeight = modal ? height - topInset - bottomInset : height;
         return state;
       });
     }
@@ -86,7 +88,7 @@ function BottomSheetHostingContainerComponent({
         }
       });
     }
-  }, [layoutState, containerLayoutState]);
+  }, [layoutState, containerLayoutState, modal, topInset, bottomInset]);
   //#endregion
 
   //#region render
diff --git a/node_modules/@gorhom/bottom-sheet/lib/module/components/bottomSheetModal/BottomSheetModal.js b/node_modules/@gorhom/bottom-sheet/lib/module/components/bottomSheetModal/BottomSheetModal.js
index 94fabdb..028a163 100644
--- a/node_modules/@gorhom/bottom-sheet/lib/module/components/bottomSheetModal/BottomSheetModal.js
+++ b/node_modules/@gorhom/bottom-sheet/lib/module/components/bottomSheetModal/BottomSheetModal.js
@@ -184,6 +184,13 @@ function BottomSheetModalComponent(props, ref) {
       });
     }
 
+    /**
+     * Exit if the modal has never been presented.
+     */
+    if (statusRef.current === MODAL_STATUS.INITIAL) {
+      return;
+    }
+
     /**
      * if the modal position is already in a closed position,
      * then we unmount the node and early exit.
diff --git a/node_modules/@gorhom/bottom-sheet/lib/typescript/components/bottomSheetHostingContainer/BottomSheetHostingContainer.d.ts b/node_modules/@gorhom/bottom-sheet/lib/typescript/components/bottomSheetHostingContainer/BottomSheetHostingContainer.d.ts
index 9498246..a2a0342 100644
--- a/node_modules/@gorhom/bottom-sheet/lib/typescript/components/bottomSheetHostingContainer/BottomSheetHostingContainer.d.ts
+++ b/node_modules/@gorhom/bottom-sheet/lib/typescript/components/bottomSheetHostingContainer/BottomSheetHostingContainer.d.ts
@@ -1,6 +1,6 @@
 import React from 'react';
 import type { BottomSheetHostingContainerProps } from './types';
-declare function BottomSheetHostingContainerComponent({ containerLayoutState, layoutState, topInset, bottomInset, shouldCalculateHeight, detached, style, children, }: BottomSheetHostingContainerProps): React.JSX.Element;
+declare function BottomSheetHostingContainerComponent({ containerLayoutState, layoutState, topInset, bottomInset, modal, shouldCalculateHeight, detached, style, children, }: BottomSheetHostingContainerProps): React.JSX.Element;
 export declare const BottomSheetHostingContainer: React.MemoExoticComponent<typeof BottomSheetHostingContainerComponent>;
 export {};
 //# sourceMappingURL=BottomSheetHostingContainer.d.ts.map
diff --git a/node_modules/@gorhom/bottom-sheet/lib/typescript/components/bottomSheetHostingContainer/types.d.ts b/node_modules/@gorhom/bottom-sheet/lib/typescript/components/bottomSheetHostingContainer/types.d.ts
index ed43773..80d8466 100644
--- a/node_modules/@gorhom/bottom-sheet/lib/typescript/components/bottomSheetHostingContainer/types.d.ts
+++ b/node_modules/@gorhom/bottom-sheet/lib/typescript/components/bottomSheetHostingContainer/types.d.ts
@@ -10,6 +10,7 @@ export interface BottomSheetHostingContainerProps
   > {
   containerLayoutState?: SharedValue<ContainerLayoutState>;
   layoutState?: SharedValue<LayoutState>;
+  modal?: boolean;
 
   shouldCalculateHeight?: boolean;
   style?: StyleProp<ViewStyle>;
diff --git a/node_modules/@gorhom/bottom-sheet/src/components/bottomSheet/BottomSheet.tsx b/node_modules/@gorhom/bottom-sheet/src/components/bottomSheet/BottomSheet.tsx
index fc52033..7930d3d 100644
--- a/node_modules/@gorhom/bottom-sheet/src/components/bottomSheet/BottomSheet.tsx
+++ b/node_modules/@gorhom/bottom-sheet/src/components/bottomSheet/BottomSheet.tsx
@@ -1084,6 +1084,46 @@ const BottomSheetComponent = forwardRef<BottomSheet, BottomSheetProps>(
         isLayoutCalculated,
       ]
     );
+
+    /**
+     * Ensure the mount position evaluation runs even when the animated
+     * reaction that normally triggers it never fires.
+     *
+     * On Reanimated v4, every `useAnimatedReaction` attached to a component
+     * instance can fail to fire when the instance mounts while the JS thread
+     * is busy: derived values (detents, isLayoutCalculated) still compute
+     * correctly, but the reaction that evaluates the initial position never
+     * runs, leaving the sheet parked off-screen at the container height.
+     *
+     * This effect re-dispatches the same evaluation from the JS side until
+     * the mount has been handled, then stops. When the reactions are healthy
+     * the first check observes `didAnimateOnMount` already true and does
+     * nothing.
+     */
+    useEffect(() => {
+      let attempts = 0;
+      const enforceMountPosition = setInterval(() => {
+        if (didAnimateOnMount.value || attempts >= 20) {
+          clearInterval(enforceMountPosition);
+          return;
+        }
+        attempts += 1;
+        runOnUI(() => {
+          'worklet';
+          if (didAnimateOnMount.value) {
+            return;
+          }
+          if (
+            animatedAnimationState.get().status === ANIMATION_STATUS.RUNNING
+          ) {
+            return;
+          }
+          evaluatePosition(ANIMATION_SOURCE.MOUNT);
+        })();
+      }, 100);
+      return () => clearInterval(enforceMountPosition);
+      // eslint-disable-next-line react-hooks/exhaustive-deps
+    }, []);
     //#endregion
 
     //#region public methods
@@ -1811,6 +1851,7 @@ const BottomSheetComponent = forwardRef<BottomSheet, BottomSheetProps>(
               containerLayoutState={containerLayoutState}
               topInset={topInset}
               bottomInset={bottomInset}
+              modal={$modal}
               detached={detached}
               style={_providedContainerStyle}
             >
diff --git a/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetHostingContainer/BottomSheetHostingContainer.tsx b/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetHostingContainer/BottomSheetHostingContainer.tsx
index 879061d..102fa1f 100644
--- a/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetHostingContainer/BottomSheetHostingContainer.tsx
+++ b/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetHostingContainer/BottomSheetHostingContainer.tsx
@@ -17,6 +17,7 @@ function BottomSheetHostingContainerComponent({
   layoutState,
   topInset = 0,
   bottomInset = 0,
+  modal,
   shouldCalculateHeight = true,
   detached,
   style,
@@ -61,6 +62,9 @@ function BottomSheetHostingContainerComponent({
         layoutState.modify(state => {
           'worklet';
           state.rawContainerHeight = height;
+          state.containerHeight = modal
+            ? height - topInset - bottomInset
+            : height;
           return state;
         });
       }
@@ -108,7 +112,7 @@ function BottomSheetHostingContainerComponent({
         });
       }
     },
-    [layoutState, containerLayoutState]
+    [layoutState, containerLayoutState, modal, topInset, bottomInset]
   );
   //#endregion
 
diff --git a/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetHostingContainer/types.d.ts b/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetHostingContainer/types.d.ts
index ed43773..80d8466 100644
--- a/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetHostingContainer/types.d.ts
+++ b/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetHostingContainer/types.d.ts
@@ -10,6 +10,7 @@ export interface BottomSheetHostingContainerProps
   > {
   containerLayoutState?: SharedValue<ContainerLayoutState>;
   layoutState?: SharedValue<LayoutState>;
+  modal?: boolean;
 
   shouldCalculateHeight?: boolean;
   style?: StyleProp<ViewStyle>;
diff --git a/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetModal/BottomSheetModal.tsx b/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetModal/BottomSheetModal.tsx
index 2954b3f..4e3a244 100644
--- a/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetModal/BottomSheetModal.tsx
+++ b/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetModal/BottomSheetModal.tsx
@@ -272,6 +272,13 @@ function BottomSheetModalComponent<T = never>(
         });
       }
 
+      /**
+       * Exit if the modal has never been presented.
+       */
+      if (statusRef.current === MODAL_STATUS.INITIAL) {
+        return;
+      }
+
       /**
        * if the modal position is already in a closed position,
        * then we unmount the node and early exit.
